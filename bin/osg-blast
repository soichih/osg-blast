#!/bin/bash

echo "Running Test"
mkdir -p test-output
osg-blast-test --config config.json --out stats.json --outdir test-output

echo "Splitting input file"
mkdir -p input
bin/osg-blast-split --config config.json --stats stats.json --outdir input
